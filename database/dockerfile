FROM postgres:18-alpine

# Environment variables are loaded from .env file at runtime (via docker-compose or --env-file)
# Do not hardcode credentials here.

# Copy the SQL schema to the Docker image
COPY OLTP.sql /docker-entrypoint-initdb.d/OLTP.sql

# Expose the default PostgreSQL port
EXPOSE 5432